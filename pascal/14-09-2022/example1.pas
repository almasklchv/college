begin
  var h, m, g: real; // инициализация переменных
  
  m := 10; // присваиваем значения переменным
  g := 9.8;
  h := 5.5;
  var A: real;  
  A := m*g*h;
  write('A = ', A); // вывод в консоль
end.    