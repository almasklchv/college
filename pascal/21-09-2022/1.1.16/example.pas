begin
  var X: integer;
  var Y: real;
  var S: string;
  var T: boolean;
  
  readln(S);
  readln(x, X);
  readln(y, Y);
  t := Y < y;
  write(s);
  writeln(Y:3:1, X:3);
  writeln(y:3:1, x:3);
  write(T)
  // при вводе 0, 1, 2, 3, 4 на каждой новой строке - будет выведено => 04.0  2
  //                                                                    4.0  2
  //                                                                    False
end.